using System;

namespace GlobalSolutionCSharp.Utils
{
    // Você pode colocar aqui métodos auxiliares para data (não obrigatório)
    public static class DateTimeHelper
    {
        // Se quiser converter "string" num DateTime de forma centralizada, etc.
    }
} 